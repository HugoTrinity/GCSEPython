myList = [0, 1, 2, 3, 4]
print(myList)

index = int(input("Input an index between 0 and 4: "))
integer = int(input("Input another integer for what you want to swap it with: "))

myList.pop(index)
myList.insert(index, integer)

print(myList)
