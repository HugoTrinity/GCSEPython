import random as r

num = r.randint(1, 10)
correct = False
while correct == False:
    guess = int(input("Guess a number between 1 and 10: "))
    if guess > num:
        print("Too high")
    if guess < num:
        print("Too low")
    if guess == num:
        print("Correct")
        correct = True
